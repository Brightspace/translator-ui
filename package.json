{
  "name": "translator-ui",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bundle": "mkdir ./src/dist && browserify --entry ./src/application.js --outfile ./src/dist/index.js",
    "start": "npm run bundle && node index.js"
  },
  "private": true,
  "author": "",
  "devDependencies": {
    "browserify": "^11.2.0"
  }
}
